# Certification status

Generated by `CatalogueSyncTest` — do not edit by hand.

| | Scenarios |
|---|---|
| In catalogue | 205 |
| Executable today | 43 |
| Disabled (remaining work) | 162 |

## Executable

| ID | Priority | Scenario |
|----|----------|----------|
| TEN-01 | P1 | `PL` provisions a new chain; chain schema is created and migrated to head version; chain admin can log in and sees zero schools. |
| TEN-02 | P1 | Chain admin creates School A (CBSE) and School B (Cambridge) under one chain; each gets its own theme, campus list, and grade ladder. |
| TEN-03 | P1 | Provisioning is re-run for an existing chain (retry after partial failure) — no duplicate schema, no data loss, migration version unchanged. |
| TEN-04 | P1 | School onboarded with a curriculum cloned from a platform template (`/clone-from-template`); the clone is independent — editing the school copy does not mutate the template. |
| TEN-05 | P2 | Two schools in the same chain define a subject with the same code `MATH`; both persist, neither collides. |
| TEN-06 | P2 | A chain is suspended / plan downgraded — feature flags gate the modules the plan excludes; existing data stays readable. |
| TEN-08 | P3 | School theme/branding change propagates to parent app, public site, and generated documents without a redeploy. |
| ACAD-01 | P1 | `AD` creates AY 2026-27 with start/end dates and marks it current; the previous AY is automatically no longer current (one-current-per-school invariant holds). |
| ACAD-04 | P1 | Grades 1–12 created with sort order; sections A/B created per grade per AY, each bound to a curriculum and strategy code. |
| ACAD-05 | P1 | A Cambridge section (`CIE-IGCSE`) and a CBSE section (`CBSE-CCE-2024`) coexist in the same school; assessment and report-card behaviour differ by strategy without config forks. |
| ACAD-08 | P2 | Curriculum tree (`curriculum_node` + `learning_outcome`) is authored and versioned; a mid-year curriculum edit does not retroactively invalidate lesson plans already delivered. |
| ADM-02 | P1 | Walk-in lead entered by `AD` with source `walkin`; same pipeline, different source attribution in funnel reporting. |
| ADM-04 | P1 | Full happy path: `lead → application_started → document_pending → fee_pending → review → test_scheduled → test_done → offered → accepted → enrolled`; each transition is recorded with actor and timestamp. |
| ENR-01 | P1 | Student enrolled into a section for an AY; exactly one `active` enrolment exists at any time. |
| ENR-03 | P1 | Mid-year section change (A → B): old enrolment ends, new one starts, attendance and marks history stays attached to the student and is attributable to the correct section per date. |
| ENR-10 | P2 | Student search across name, admission no, guardian phone returns results scoped strictly to the caller's school. |
| TT-02 | P1 | Weekly timetable built for a section; a slot that double-books a teacher is rejected with a clear message. (Implemented — `TimetableRepository` clash check.) |
| TT-06 | P1 | Teacher's personal timetable view (`/teachers/{id}`) matches the sum of their section slots, across sections and grades. |
| ATT-01 | P1 | Class teacher marks day-level attendance for a section; re-submitting the same day updates rather than duplicating (day-level uniqueness holds — V010). |
| ATT-02 | P1 | Subject teacher marks period-level attendance; day-level and period-level records for the same date coexist without conflict. |
| ATT-07 | P1 | Biometric/RFID device pushes student events (`/{deviceId}/events/student`); duplicate events for the same student/day are idempotent; device clock skew does not create a next-day record. |
| ASMT-01 | P1 | CBSE flow: PT1 / HY / Annual assessments created per the strategy, with components and weights summing to 100%. |
| LMS-01 | P1 | Teacher publishes an assignment with a due date and attachment; the section's students and their parents see it. |
| LMS-03 | P1 | Teacher grades a submission with feedback; the grade appears to the student and parent. |
| LMS-05 | P2 | Lesson plan created against curriculum nodes, moved through its status workflow, and visible to the HM for review. |
| LMS-06 | P1 | Content item uploaded (video/PDF) via the upload ticket flow; download ticket is time-bound and does not leak cross-tenant. |
| LMS-08 | P1 | Assignment for a section is not visible to students of another section or school. |
| FEE-06 | P1 | Duplicate gateway callback with the same idempotency key does not double-credit. (Idempotency key is unique on `payment` — verify the service path honours it.) |
| FEE-07 | P1 | Partial payment moves the invoice to `partial`; the balance and the next-due amount are correct in the parent app. |
| COMM-01 | P1 | Announcement targeted at a school / grade / section reaches exactly that audience; read receipts tracked. |
| COMM-02 | P1 | Teacher–parent thread: message, reply, and unread counts behave correctly on both apps. |
| COMM-03 | P1 | Push notification delivered to a registered device; token invalidation on logout/uninstall stops delivery. |
| COMM-10 | P1 | Message content is scoped per tenant — no cross-school thread visibility under any role. |
| TRN-01 | P1 | Route with ordered stops created; student assigned to a stop; route roster matches assignments. |
| TRN-04 | P1 | GPS pings stream during a trip; geofence entry/exit fires the arrival notification once, not repeatedly. |
| LIB-01 | P2 | Title and copies catalogued; copy issued to a student with a due date; return closes the issue. |
| STF-06 | P1 | HM/principal view aggregates across all sections in their school and nothing beyond it. |
| SEC-02 | P1 | Access token expiry mid-session → transparent refresh or clean redirect to login, on every app. (Known open item.) |
| SEC-04 | P1 | Row-level security holds: a token for School A cannot read School B's students, marks, invoices, or messages, including via ID enumeration. |
| SEC-05 | P1 | Chain HQ role can read across the chain's schools but cannot read another chain. |
| SEC-07 | P1 | File upload/download tickets are tenant-scoped, expiring, and non-guessable. |
| DEV-02 | P1 | Device event ingestion is idempotent and tolerant of out-of-order delivery. |
| NFR-07 | P1 | Migration of a chain schema from version N to N+1 on a populated database: no downtime beyond the agreed window, no data loss, rollback path tested. |

## Disabled

| ID | Priority | Blocked by |
|----|----------|-----------|
| TEN-07 | P2 | GAP-24 — campus is decorative: section/staff/timetable_slot carry no campus_id, so campus-scoped structure and a campus-level admin role cannot be expressed (Phase 1). |
| ACAD-02 | P1 | GAP-25 — no AY/term date validation: a term outside its academic year, or overlapping terms, are both accepted (Phase 1). |
| ACAD-03 | P2 | GAP-25 — academic years may overlap; no EXCLUDE constraint on daterange (Phase 1). |
| ACAD-06 | P1 | GAP-10 — section.capacity is stored but never checked at enrolment or admission offer, and there is no over-capacity override with a reason (Phase 2). |
| ACAD-07 | P2 | No setup-readiness surface: section_subject_teacher records a primary teacher, but nothing reports sections missing one before term start. New gap found in Phase 0. |
| ACAD-09 | P1 | GAP-05 — subjects bind to sections only; there is no student_subject election, so marks, timetable and report cards cannot follow a student's own subject set (Phase 2). |
| ACAD-10 | P1 | GAP-01 — no working-day pattern or calendar master, so nothing computes a working-day denominator or shifts a due date (Phase 1). |
| ADM-01 | P1 | Application is created correctly with source 'website', but nothing acknowledges it: no module publishes a domain event or calls NotificationService, so no dispatch row is written. New gap found in Phase 0 — notification producers are unwired. |
| ADM-03 | P1 | GAP-16 — /admissions/track returns the stage, but pending documents are loose JSONB with no verification state, so the guardian cannot be shown what is outstanding or what to do next. |
| ADM-05 | P1 | No server-side admissions state machine: AdmissionsRepository.transition writes any target state, so `lead → enrolled` is accepted. New gap found in Phase 0. |
| ADM-06 | P2 | No cohort test scheduling, no bulk score capture, and no rank list against seats per grade. New gap found in Phase 0 (adjacent to GAP-11). |
| ADM-07 | P1 | GAP-11 — offer_expires_on is stored but no job expires an offer or returns the seat to the pool. |
| ADM-08 | P2 | GAP-11 — no waitlist promotion on decline. |
| ADM-09 | P2 | GAP-11 — no duplicate-lead detection by guardian phone; a second enquiry creates a second funnel entry. |
| ADM-10 | P1 | Conversion creates the student and the enrolment and links converted_student_id, but no guardian is created or linked, so the family has no login after admission. New gap found in Phase 0. |
| ADM-11 | P1 | GAP-09 — no family grouping, no sibling concession, no combined invoice (Phase 4). |
| ADM-12 | P1 | GAP-16 — admission documents are loose JSONB with no per-document verification state or reviewer, and enrolment is not gated on them (Phase 8). |
| ADM-13 | P1 | An applicant has no student row until conversion and fee_invoice.student_id is NOT NULL, so the admission fee cannot be invoiced at fee_pending. New gap found in Phase 0. |
| ADM-14 | P2 | Rejection transition works, but the guardian notification has no producer (see ADM-01). New gap found in Phase 0. |
| ADM-15 | P2 | No funnel analytics endpoint: conversion by source/stage, drop-off and time-in-stage are not computed anywhere. New gap found in Phase 0. |
| ADM-16 | P1 | GAP-01 + GAP-09 — a mid-year joiner needs a working-day denominator from the join date and a pro-rated fee schedule; neither exists (Phases 1 and 4). |
| ENR-02 | P1 | GAP-26 — roll_no is free text with no uniqueness constraint and no generator, and admission numbers have no scheme; nothing renumbers a section after a transfer (Phase 2). |
| ENR-04 | P1 | GAP-18 — guardian_student distinguishes primary/secondary and custody, but a non-guardian authorised pickup contact cannot be modelled at all (Phase 8). |
| ENR-05 | P2 | GAP-18 — custody restrictions on pickup have nowhere to live (Phase 8). |
| ENR-06 | P1 | GAP-17 — no health/emergency model: conditions, allergies and prioritised emergency contacts are not stored, so they cannot reach the class teacher or the driver view (Phase 8). |
| ENR-07 | P3 | GAP-16 — no student document/photo store, so no ID-card generation for a section (Phase 8). |
| ENR-08 | P2 | GAP-02 — re-admission after withdrawal depends on rollover-aware enrolment history (Phase 6). |
| ENR-09 | P1 | GAP-23 — no CSV bulk import for students/staff/marks and no per-row validation (Phase 8). |
| CAL-01 | P1 | GAP-01 — no school_calendar or working_day_pattern table; the annual calendar cannot be published at all (Phase 1). |
| CAL-02 | P1 | GAP-01 — attendance accepts any date, and no percentage anywhere subtracts holidays from the denominator (Phase 1). |
| CAL-03 | P1 | GAP-01 — the timetable read has no calendar to consult, so a holiday renders as a normal school day (Phase 1). |
| CAL-04 | P1 | GAP-01 — no closure declaration, so marked attendance cannot be voided and no parent notification is raised (Phase 1). |
| CAL-05 | P2 | GAP-01 — a calendar entry has no grade scope (Phase 1). |
| CAL-06 | P2 | GAP-01 + GAP-24 — a calendar entry has no campus scope, and campus is decorative (Phase 1). |
| CAL-07 | P2 | GAP-01 — no calendar to expose to the parent app or the public site (Phase 1). |
| CAL-08 | P2 | Announcements carry read receipts, but there is no event entity and no RSVP, so reach cannot be reconciled against an event roster. New gap found in Phase 0. |
| TT-01 | P1 | GAP-12 — no bell_schedule/period master; every slot carries its own free-text times (Phase 2). |
| TT-03 | P2 | GAP-12 — the clash query is teacher-only; two sections can be scheduled into the same room at the same time (Phase 2). |
| TT-04 | P3 | GAP-12 — no configured weekly teacher load and no publish-time warning (Phase 2). |
| TT-05 | P1 | Slots carry effective_from/effective_to, but every read ignores them: TimetableRepository.forSection/forTeacher select all rows for the section, so a revision rewrites history instead of superseding it from a date. New gap found in Phase 0. |
| TT-07 | P2 | No day view: the timetable API returns the full week for a section with no date filter and no after-hours suppression, so a parent/student 'today' view cannot be served. New gap found in Phase 0. |
| TT-08 | P1 | GAP-07 — staff absence and timetable slots are unrelated: no cover assignment, no substitute view, no permission for the substitute to mark that period (Phase 3). |
| TT-09 | P1 | GAP-06 + GAP-01 — there is no exam timetable entity and no calendar to mark an exam week with, so the regular timetable cannot be suppressed (Phases 1 and 5). |
| TT-10 | P2 | No section-delete endpoint exists, so the blocked-or-cascade behaviour has no surface to certify. New gap found in Phase 0. |
| ATT-03 | P1 | No producer raises an absence event: nothing calls DomainEvents or NotificationService from the attendance path, so no parent notification is dispatched and the duplicate-suppression rule has nothing to suppress. New gap found in Phase 0. |
| ATT-04 | P1 | GAP-01 — statuses persist correctly, but no monthly attendance percentage is computed anywhere (the dashboard reports today's raw present count only), so late/half-day/excused weighting has no consumer (Phase 1). |
| ATT-05 | P1 | GAP-08 — approving a leave application updates leave_application only; no attendance records are materialised for the covered days (Phase 3). |
| ATT-06 | P1 | GAP-08 + GAP-27 — the upsert overwrites the prior value with no amendment record, no approval and no audit row (Phase 3). |
| ATT-08 | P1 | Device replay lands on the supplied date, but the upsert unconditionally overwrites: a manual correction made in the interim is replaced by the replayed 'present'. There is no source precedence rule. New gap found in Phase 0. |
| ATT-09 | P2 | No offline sync protocol: marks carry no client version or timestamp, so a conflicting server-side edit is silently overwritten rather than surfaced. New gap found in Phase 0. |
| ATT-10 | P1 | GAP-01 — no working-day denominator and no enrolment-window-aware percentage for a mid-year joiner or leaver (Phase 1). |
| ATT-11 | P2 | No chronic-absence report: nothing aggregates attendance over a rolling window per section or grade. New gap found in Phase 0. |
| ATT-12 | P1 | Attendance accepts a future date and a date outside the student's enrolment window; neither is validated in AttendanceRepository.mark. New gap found in Phase 0. |
| ATT-13 | P2 | GAP-08 — staff punches are recorded, but an approved staff leave does not surface as `leave` in staff_attendance (Phase 3). |
| ASMT-02 | P1 | Cambridge assessments persist with their own strategy code, but no grade is computed at all: mark.grade_letter is whatever the caller supplies, so a CIE scale cannot be applied independently of CBSE. Needs the strategy grade computation of GAP-29 (Phase 5). |
| ASMT-03 | P1 | No weight validation: components whose weights do not sum to the assessment total are accepted silently, and nothing flags it before marking opens. New gap found in Phase 0. |
| ASMT-04 | P1 | GAP-06 — there is no bulk mark endpoint, no max_marks validation on entry, and a blank mark is indistinguishable from a zero (mark.status does not exist) (Phase 5). |
| ASMT-05 | P1 | GAP-06 — no absent semantics on a mark, so an exam absence cannot be excluded from averages or rendered as AB (Phase 5). |
| ASMT-06 | P1 | The lifecycle statuses persist, but nothing enforces them: AssessmentRepository.enterMark upserts a mark regardless of the assessment being `locked`. New gap found in Phase 0. |
| ASMT-07 | P1 | GAP-06 + GAP-27 — unlocking is an unguarded status write with no role check, no reason and no audit row (Phase 5). |
| ASMT-08 | P2 | GAP-06 — no mark_revision: a moderated mark overwrites the original rather than superseding it, and nothing regenerates the report card (Phase 5). |
| ASMT-09 | P1 | GAP-06 — no exam_schedule/exam_session entity: no per-student paper clash check, no room or invigilator allocation, no hall tickets (Phase 5). |
| ASMT-10 | P1 | GAP-13 — report_card.payload is free-form JSON with no content model: no subject rows, attendance summary, co-scholastic ratings, remarks or promotion decision (Phase 5). |
| ASMT-11 | P2 | GAP-29 — no rank, percentile or grade-boundary computation (Phase 5). |
| ASMT-12 | P1 | Locking a report card sets is_locked, but generateReportCard always inserts a new row, so a locked card can be silently superseded by a regenerated one. New gap found in Phase 0. |
| ASMT-13 | P1 | GAP-05 — no student_subject election, so a report card cannot show a student's own subject set (Phase 2). |
| ASMT-14 | P2 | GAP-13 — without a report-card content model there is nowhere to express 'terms attended' or the explanatory note for a mid-year joiner (Phase 5). |
| ASMT-15 | P2 | No dues-block policy: mark publication and fee status are unconnected, and the withhold-vs-release choice is not configurable. New gap found in Phase 0. |
| LMS-02 | P1 | Submissions record a timestamp but nothing compares it to the assignment's due date: assignment_submission has no late flag and no late policy. New gap found in Phase 0. |
| LMS-04 | P2 | Quiz attempts are stored, but the score is supplied by the caller: no auto-scoring against the stored answers and no reattempt policy. New gap found in Phase 0 (authoring UI is already in the backlog). |
| LMS-07 | P3 | LTI launch and grade passback are stubbed; no adapter exists to certify against (already in the backlog). |
| FEE-01 | P1 | fee_structure and fee_structure_line exist in the schema, but no endpoint creates or reads them: FeesController covers heads, invoices, payments and ledger only. A school cannot define next year's structure at all. New gap found in Phase 0 (Phase 4 owns the fee engine). |
| FEE-02 | P1 | GAP-09 — no bulk invoice generation from a fee structure and no fee_schedule_run, so re-running cannot be proved idempotent (Phase 4). |
| FEE-03 | P1 | fee_concession rows have no endpoint and invoice creation never consults them, so a concession cannot reduce an invoice or appear as a line. New gap found in Phase 0 (Phase 4). |
| FEE-04 | P2 | GAP-09 — no family grouping and no combined family invoice (Phase 4). |
| FEE-05 | P1 | GAP-09 — no checkout initiation and no gateway callback handler; only manually recorded payments exist (Phase 4, gateway credentials pending). |
| FEE-08 | P1 | GAP-09 — no fee_adjustment: a cheque bounce cannot be reversed, only deleted (Phase 4). |
| FEE-09 | P1 | GAP-09 — nothing transitions an invoice to overdue and there is no reminder cadence; the only scheduled job in the codebase is the outbox publisher (Phase 4). |
| FEE-10 | P2 | GAP-09 + GAP-27 — no late-fee policy, no waiver path, no audit on waiver (Phase 4). |
| FEE-11 | P1 | GAP-09 — status 'refunded' exists but nothing sets it: no refund or credit-note endpoint and no pro-rating on withdrawal (Phase 4). |
| FEE-12 | P2 | GAP-09 + GAP-30 — transport fees are not derived from a route assignment, so a mid-year route change adjusts nothing (Phase 4). |
| FEE-13 | P2 | GST is stored per line but never computed: fee_head.gst_rate_pct is not applied at invoice creation, and no IRN fields are populated. New gap found in Phase 0. |
| FEE-14 | P1 | No day-book or collection report: the ledger endpoint reads one source id at a time and nothing aggregates payments over a date range. New gap found in Phase 0. |
| FEE-15 | P1 | No outstanding-dues report by grade/section/student, and no year-end dues flag. New gap found in Phase 0 (Phase 4 produces the arrears balance rollover consumes). |
| FEE-16 | P1 | GAP-02 + GAP-09 — no rollover, so arrears cannot carry into next year's opening balance (Phase 6). |
| FEE-17 | P2 | recordPayment reads invoice.paid and writes back paid + amount with no guard: two simultaneous payments lose an update and can over-credit, and there is no advance-payment path. New gap found in Phase 0. |
| COMM-04 | P2 | GAP-21 — notification_dispatch records a status but nothing retries a failure and there is no dispatch-log surface (Phase 8). |
| COMM-05 | P2 | GAP-21 — guardian opt-in flags exist per channel, but there are no quiet hours, no category mute and no emergency override (Phase 8). |
| COMM-06 | P1 | An emergency announcement can be created and published, but no module fans it out to channels: NotificationService has no callers, so there are no delivery stats to meet an SLA against. New gap found in Phase 0. |
| COMM-07 | P2 | WhatsApp BSP adapter is stubbed pending credentials (already in the backlog). |
| COMM-08 | P1 | GAP-03 — no withdrawal workflow and no single enrolment-active-on-date predicate, so a withdrawn student's parent keeps receiving section communications (Phase 7). |
| COMM-09 | P2 | GAP-20 — no PTM slot publication or parent booking (Phase 8). |
| TRN-02 | P2 | GAP-30 — route capacity is never checked against the assigned vehicle's capacity (Phase 8). |
| TRN-03 | P1 | A trip can be started, checked into and ended, but boarding and alighting raise no parent notification: nothing calls NotificationService from the transport path. New gap found in Phase 0. |
| TRN-05 | P2 | GAP-30 — nothing compares class attendance with bus boarding, so no mismatch alert exists (Phase 8). |
| TRN-06 | P2 | GAP-30 + GAP-09 — student_transport carries starts_on/ends_on but nothing effective-dates a change through the roster or the fee schedule (Phases 4 and 8). |
| TRN-07 | P2 | Check-in writes into the trip manifest with no client-supplied event id, so a queued offline check-in cannot be de-duplicated on replay. New gap found in Phase 0. |
| TRN-08 | P3 | No trip reassignment path for a breakdown or driver substitution mid-route, and no parent notification. New gap found in Phase 0. |
| TRN-09 | P2 | GAP-03 — withdrawal has no workflow, so nothing removes a withdrawn student from the route roster (Phase 7). |
| LIB-02 | P3 | GAP-22 — no per-grade issue limits (Phase 4 posts library charges to the ledger; limits ride along with it). |
| LIB-03 | P2 | GAP-22 — library_issue.fine is a column nothing computes, and no fine posts to the fee ledger (Phase 4). |
| LIB-04 | P3 | GAP-22 — no lost/damaged charge path; the copy status exists but no charge is raised (Phase 4). |
| LIB-05 | P2 | GAP-03 + GAP-22 — no year-end clearance workflow to block a student with an unreturned copy (Phase 7). |
| STF-01 | P1 | There is no staff-creation endpoint (people module exposes reads only), and assignSectionSubjectTeacher accepts any staff id without checking for a teaching role. New gap found in Phase 0. |
| STF-02 | P2 | GAP-08 — leave is applied and decided, but approval writes nothing into staff_attendance (Phase 3). The approver is also unchecked: any staff id may be passed as approverStaffId. |
| STF-03 | P1 | GAP-07 — approved teacher leave does not surface the affected periods for substitution (Phase 3). |
| STF-04 | P1 | GAP-27 — no staff-exit path: nothing revokes access on the last working day or reassigns section duties, and role revocation is not audited (Phase 3). |
| STF-05 | P1 | Row-level security is school-scoped only: a teacher's token can read any section, student or mark in their school, including another teacher's. Teacher-scope enforcement does not exist. New gap found in Phase 0 — security-relevant. |
| SEC-01 | P1 | A used OTP is consumed and a wrong code is refused, but there is no rate limiting on /v1/auth/otp/verify and OtpStore accepts the literal code 000000 unconditionally — the dev bypass is not gated on a profile or property despite its own doc comment. New gap found in Phase 0 — security-relevant. |
| SEC-03 | P1 | /v1/iam/me/screens reports the caller's screens, but no endpoint checks them: a hand-crafted call to any module succeeds for any authenticated staff account regardless of role. Screen access is advisory only. New gap found in Phase 0 — security-relevant. |
| SEC-06 | P1 | Platform-admin actions authenticate separately (platform.platform_user + its own OTP flow) but write no audit trail: audit_log lives in the chain schema and ChainAdminController records nothing. New gap found in Phase 0 — security-relevant. |
| SEC-08 | P1 | GAP-27 — AuditService.record exists but no high-risk mutation calls it: mark unlock, fee waiver, enrolment status change and role grant all write without an audit row (Phase 3). |
| SEC-09 | P1 | GAP-23 — consent_record exists, but there is no export, erasure or retention path to serve a DPDP request (Phase 8). |
| SEC-10 | P1 | Nothing scopes a guardian to their own children: /v1/people/students returns every student in the school for any authenticated caller, guardians included. New gap found in Phase 0 — security-relevant. |
| YEC-01 | P1 | GAP-02 — no readiness check: nothing lists unpublished assessments, unlocked report cards, unmarked days or outstanding dues before closure (Phase 6). |
| YEC-02 | P1 | GAP-02 — no structure clone into the next AY (Phase 6). |
| YEC-03 | P1 | GAP-02 — enrolment.status allows 'promoted' but nothing sets it; there is no bulk promotion (Phase 6). |
| YEC-04 | P1 | GAP-02 — no detain path, because there is no promotion decision to act on (Phases 5 and 6). |
| YEC-05 | P2 | GAP-02 + GAP-10 — no reshuffle rules, and capacity is not enforced anywhere (Phases 2 and 6). |
| YEC-06 | P1 | GAP-02 — nothing carries fee arrears, library dues, transport assignment, guardian links or medical info into the next year (Phase 6). |
| YEC-07 | P1 | GAP-02 — no rollover_run, so idempotency and roll-back before activation have nothing to key on (Phase 6). |
| YEC-08 | P1 | GAP-14 — academic_year has only is_current: a closed year stays mutable and there is no authorised reopen path (Phase 1). |
| YEC-09 | P1 | GAP-02 — historical reporting across two rollovers cannot be exercised until a rollover exists (Phase 6). The fixture already carries one prior year of history for this scenario to assert against once it does. |
| YEC-10 | P2 | GAP-02 — teacher assignments cannot fail to carry forward until something carries anything forward (Phase 6). |
| YEC-11 | P2 | GAP-02 — no rollover to time or restart (Phase 6). The bulk seed for this scenario is available via -Dschoolsoft.cert.bulk-students. |
| XFER-01 | P1 | GAP-03 — enrolment.status can be set to 'withdrawn', but there is no withdrawal entity, no reason, no last-working-date and no clearance checklist (Phase 7). |
| XFER-02 | P1 | GAP-03 — no certificate entity and no TC generation with statutory fields or a serial number (Phase 7, using the number series from Phase 2). |
| XFER-03 | P1 | GAP-03 — there is no single enrolment-active-on-date predicate, so setting a status to 'withdrawn' does not remove the student from rosters, timetable, transport or communications (Phase 7). |
| XFER-04 | P1 | GAP-03 + GAP-04 — no post-withdrawal access scope and no revocation window (Phase 7). |
| XFER-05 | P1 | GAP-15 — /enrolments/{id}/transfer moves a section only; there is no intra-chain school transfer that preserves history and settles the source ledger (Phase 7). |
| XFER-06 | P2 | GAP-16 — no document store for prior-school records, so external marks cannot be held as historical context (Phase 8). |
| XFER-07 | P2 | GAP-03 — with no certificate entity there is no duplicate-issue prevention (Phase 7). |
| XFER-08 | P1 | GAP-03 — no clearance probe against fees, so withdrawal with dues is neither blocked nor overridable with a reason (Phase 7). |
| GRAD-01 | P1 | GAP-02 — graduation is an outcome of rollover, which does not exist: nothing closes a terminal-grade cohort as 'graduated' (Phase 6). |
| GRAD-02 | P1 | GAP-03 — no certificate entity, so no school-leaving certificate or final transcript (Phase 7). |
| GRAD-03 | P2 | Board integration is a stub adapter and there is no transcript to merge results onto (GAP-13, Phase 5; adapters remain credential-blocked). |
| GRAD-04 | P2 | GAP-04 — no alumni identity or scope downgrade after graduation (Phase 7). |
| GRAD-05 | P2 | GAP-04 + GAP-14 — no retention window, no audited alumni document request, and closed years are not read-only (Phases 1 and 7). |
| GRAD-06 | P1 | GAP-02 — with no rollover, graduating students cannot be excluded from next year's fee generation, transport roster or communications (Phase 6). |
| OPS-01 | P1 | GAP-18 — no gate pass or early-dismissal approval chain, and no half-day write-back (Phase 8, on top of Phase 3's amendment path). |
| OPS-02 | P1 | GAP-18 — no authorised-pickup list to enforce at dismissal and nowhere to log a refusal (Phase 8). |
| OPS-03 | P3 | GAP-18 — no visitor log (Phase 8). |
| OPS-04 | P2 | GAP-19 — no discipline incident record, which also leaves the TC conduct line unbacked (Phase 8). |
| OPS-05 | P2 | GAP-17 — no infirmary visit log and no medical history to restrict access to (Phase 8). |
| OPS-06 | P2 | GAP-19 — no counselling notes and no restricted access class to hold them under (Phase 8). |
| OPS-07 | P3 | GAP-18 — no evacuation roster built from live attendance (Phase 8). |
| DEV-01 | P1 | GAP-24 — a device is registered to a school but there is no campus to bind it to. The unregistered-device half already holds (an unknown device id is rejected); campus scoping arrives in Phase 1. |
| DEV-03 | P2 | device.last_seen_at is updated on every event, but nothing surfaces a device that has stopped reporting: no heartbeat check and no alert before the day's attendance is affected. New gap found in Phase 0. |
| DEV-04 | P3 | A device belongs to exactly one school and the ingest trusts the schoolId in the request body rather than the device's own registration, so a shared-campus device has no routing rule to certify. New gap found in Phase 0. |
| INT-01 | P2 | A job can be enqueued and processed to 'completed' against the stub adapter, but a failed job is terminal: process() accepts only status 'queued' and nothing re-queues, so the retry-without-duplication half cannot pass. New gap found in Phase 0. |
| INT-02 | P2 | GAP-05 — an export payload cannot represent students whose subject set differs from their section's, because student-level election does not exist (Phase 2). |
| INT-03 | P3 | UDISE+/CIE Direct adapters are stubs pending credentials (already in the backlog). |
| INT-04 | P3 | No accounting export (Tally/Zoho) exists (already in the backlog). |
| NFR-01 | P1 | Perf profile: needs the bulk seed (-Dschoolsoft.cert.bulk-students=2000) and an agreed p95 target. Note the correctness half already has a finding — concurrent writes to the same attendance day rely on the upsert alone, with no conflict surface (see ATT-09). |
| NFR-02 | P1 | Perf profile: report-card publication for 2,000 students needs the bulk seed and the report-card content model of GAP-13 (Phase 5). |
| NFR-03 | P1 | GAP-09 + GAP-21 — invoice generation and notification fan-out do not exist as jobs, so there is no queue to measure backlog on (Phases 4 and 8). |
| NFR-04 | P2 | Client-side scenario: low-end Android over 3G and tablet layout are measured against the parent app build, not the API suite. |
| NFR-05 | P2 | Chain-level dashboards over ten schools need the multi-school perf fixture; chain HQ analytics is Phase 2 of the design doc and out of scope for this remediation. |
| NFR-06 | P1 | Backup and point-in-time restore are exercised by the ops runbook against a real cluster; there is nothing in the application to certify them against. |
| NFR-08 | P1 | Date derivation uses the JVM default zone, not the school's: DeviceController falls back to LocalDate.now() and EnrolmentRepository.transfer uses LocalDate.now(), while school.timezone is stored and never read. On a UTC server a 23:55 IST event lands on the next day. New gap found in Phase 0. |
| NFR-09 | P2 | No localisation layer: currency and date formatting live in each frontend, and there are no generated documents yet to check (report card rendering is Phase 5). |
| NFR-10 | P2 | No alerting path: failures are logged, but nothing raises an actionable alert carrying tenant context. New gap found in Phase 0. |
