/**
 * Shared front-end pieces. One so far: the sign-in code step, extracted before
 * the second surface copied it rather than after the fifth.
 */
export { CodeStep } from "./code-step";
export type { CodeStepProps } from "./code-step";
