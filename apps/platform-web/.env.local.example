# Base URL of the Schoolsoft API (apps/api). No trailing slash.
NEXT_PUBLIC_SCHOOLSOFT_API_URL=http://localhost:8080

# Where school staff and chain HQ sign in — linked from the operator door so
# somebody at the wrong one is not stuck. Dev defaults to localhost:3001.
NEXT_PUBLIC_SCHOOL_WEB_URL=http://localhost:3001/login
