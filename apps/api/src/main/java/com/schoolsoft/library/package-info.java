/**
 * Library (per design doc §7 Layer 4.8). Catalogue (title) &rarr; physical
 * copies &rarr; issue/return with fine accrual. Kept deliberately simple per
 * §19's MVP-cut guidance ("Library: manual + simple catalog" if scope needs
 * trimming) — no MARC import, no reservations queue.
 */
package com.schoolsoft.library;
