#!/bin/sh
# Minimal Maven launcher. Uses system mvn if present, else downloads a wrapper.
set -e
if command -v mvn >/dev/null 2>&1; then
  exec mvn "$@"
fi
echo "mvn not found on PATH. Install Maven 3.9+ (brew install maven) or use mvnd." >&2
exit 1
