import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.schoolsoft.parent",
  appName: "Schoolsoft Parent",
  // `next build` with output: "export" emits the static bundle here.
  webDir: "out",
};

export default config;
